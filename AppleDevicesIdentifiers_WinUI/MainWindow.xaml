<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AppleDevicesIdentifiers_WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppleDevicesIdentifiers_WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Apple Devices Identifiers">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="10,10,10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="CustomTitleBar" Text="Apple Device Identifiers" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="28"/>
        </Grid>

        <StackPanel Grid.Row="1" Margin="20,20,20,0">
            <TextBlock Margin="0,0,0,10" TextWrapping="Wrap">
                <Run Text="Updated "/>
                <Run Text="2026-07" FontWeight="Bold"/>
                <LineBreak/>
                <Run Text="Special Thanks: GitHub: "/>
                <Run Text="pluwen/apple-device-model-list" FontWeight="Bold"/>
            </TextBlock>

            <TextBlock Text="Device Type" Margin="0,0,0,5"/>
            <ComboBox x:Name="CmbType" SelectionChanged="CmbType_SelectionChanged" HorizontalAlignment="Stretch">
                <ComboBoxItem Content="iPhone" Tag="iPhone"/>
                <ComboBoxItem Content="iPad" Tag="iPad"/>
                <ComboBoxItem Content="iPod" Tag="iPod"/>
                <ComboBoxItem Content="Apple TV" Tag="Apple TV"/>
                <ComboBoxItem Content="Apple Watch" Tag="Apple Watch"/>
                <ComboBoxItem Content="HomePod" Tag="HomePod"/>
            </ComboBox>

            <TextBlock Text="Device Marketing Name" Margin="0,20,0,5"/>
            <ComboBox x:Name="CmbModel" SelectionChanged="CmbModel_SelectionChanged" HorizontalAlignment="Stretch"/>

            <Border Margin="0,30,0,0" Padding="15" CornerRadius="8" 
                    BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}">
                <StackPanel>
                    <TextBlock Text="Identifier" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,10"/>
                    <TextBlock Text="Your device identifier is:" Margin="0,0,0,5"/>
                    <Border Background="AliceBlue" CornerRadius="15" Height="60">
                        <TextBlock x:Name="LblIdentifier" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black"/>
                    </Border>
                </StackPanel>
            </Border>

        </StackPanel>
    </Grid>
</Window>
